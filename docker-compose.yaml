services:
  app:
    build: .
    container_name: panghu-image-web
    env_file: .env
    ports:
      - "${API_PORT:-8787}:8787"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
